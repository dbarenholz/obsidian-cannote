/**
 * Represents a point
 *
 * @interface IPoint
 * @field {number} x coordinate of the point
 * @field {number} y coordinate of the point
 */
export interface IPoint {
    x: number;
    y: number;
}